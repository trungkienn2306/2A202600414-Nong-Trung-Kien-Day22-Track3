# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Jupyter
.ipynb_checkpoints/
# NOTE: notebooks/*.ipynb IS committed for submission (with output cells preserved
# for grading). The Jupytext `.py` files are the source-of-truth template;
# `.ipynb` is the student's executed deliverable.

# Lab artifacts (keep lightweight submission artifacts; ignore large regenerated weights)
data/pref/*
!data/pref/*.parquet
data/eval/*
!data/eval/*.json
!data/eval/*.jsonl
data/eval_prompts.json
adapters/sft-mini/*
!adapters/sft-mini/adapter_config.json
adapters/dpo/*
!adapters/dpo/adapter_config.json
!adapters/dpo/dpo_metrics.json
adapters/orpo/
adapters/**/*.safetensors
adapters/**/tokenizer.json
adapters/**/tokenizer_config.json
adapters/**/chat_template.jinja
adapters/**/README.md
gguf/
*.gguf
runs/
wandb/

# HuggingFace cache (Colab + local — model weights are huge)
.cache/
hf_cache/

# Env / secrets
.env
.env.local
*.token

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
